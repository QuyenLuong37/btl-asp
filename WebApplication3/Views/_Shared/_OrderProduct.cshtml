@*<select name="SortBy" id="SortBy" onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "name_asc"}) + "'");">
        <option value="manual" onchange="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "option"}) + "'");">Tùy chọn</option>
        <option value="best-selling" onchange="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "soldest"}) + "'");">Sản phẩm bán chạy</option>
        <option value="title-ascending" onchange="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "name_asc"}) + "'");">Theo bảng chữ cái từ A-Z</option>
        <option value="title-descending" onchange="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "name_desc"}) + "'");">Theo bảng chữ cái từ Z-A</option>
        <option value="price-ascending">Giá từ thấp tới cao</option>
        <option value="price-descending">Giá từ cao tới thấp</option>
        <option value="created-descending">Mới nhất</option>
        <option value="created-ascending">Cũ nhất</option>
    </select>*@
<style>
    #menuSort {
        position: absolute;
        z-index: 1000;
        padding: 0;
        list-style: none;
        width: 100%;
        margin-top: .25rem;
        background: #fff;
        border: 1px solid #999;
        transition: all .5s;
    }
        #menuSort li {
            padding: 0.5rem .5rem .5rem .5rem;
            cursor: pointer;
        }
        #menuSort li:hover {
            background: blue;
            color: #fff;
        }
</style>
<div class="position-relative">
    <div onclick="showMenuSort()" class="d-flex align-items-center" style="width:300px">
        <span class="mr-2">Sắp xếp:  </span>
        <span style="padding: .25rem;border: 1px solid #999;flex: 1"><span>@ViewBag.sortActive</span> <i class="fas fa-chevron-down ml-2"></i></span>
    </div>
    <ul id="menuSort" style="display: none;width:300px">
        <li onclick='@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "option", sortActive="Tùy chọn"}) + "'");'>Tùy chọn</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "soldest", sortActive="Sản phẩm bán chạy"}) + "'");">Sản phẩm bán chạy</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "name_asc", sortActive="Theo bảng chữ cái từ A-Z"}) + "'");">Theo bảng chữ cái từ A-Z</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "name_desc", sortActive="Theo bảng chữ cái từ Z-A"}) + "'");">Theo bảng chữ cái từ Z-A</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "price_asc", sortActive="Giá từ thấp tới cao"}) + "'");">Giá từ thấp tới cao</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "price_desc", sortActive="Giá từ cao tới thấp"}) + "'");">Giá từ cao tới thấp</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "newest", sortActive="Mới nhất"}) + "'");">Mới nhất</li>
        <li onclick="@("window.location.href='" + @Url.Action(ViewBag.action, "Collection", new { price = ViewBag.price, filterType = ViewBag.filterType, size = ViewBag.size, category =  ViewBag.title, sortOrder = "oldest", sortActive="Cũ nhất"}) + "'");">Cũ nhất</li>
    </ul>
</div>


